const $=s=>document.querySelector(s);
const esc=s=>String(s??'').replace(/[&<>'\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','\"':'&quot;'}[c]));
const money=(v,c='RUB')=>new Intl.NumberFormat('ru-RU',{style:'currency',currency:c||'RUB',maximumFractionDigits:0}).format(Number(v)||0);
const total=p=>Object.values(p.stocks||{}).reduce((a,b)=>a+(Number(b)||0),0);
async function init(){const sku=new URLSearchParams(location.search).get('sku');const [stockRes,specRes]=await Promise.all([fetch(`data/stock.json?v=${Date.now()}`,{cache:'no-store'}),fetch(`data/specs.json?v=${Date.now()}`,{cache:'no-store'})]);const stock=await stockRes.json();const specs=await specRes.json();const p=stock.products.find(x=>String(x.sku)===String(sku));const s=specs[String(sku)];if(!p){$('#productView').innerHTML='<div class="not-found">Товар не найден.</div>';return}document.title=`${p.name} — характеристики`;const rows=Object.entries(p.stocks||{}).filter(([,v])=>Number(v)>0).map(([c,v])=>`<div class="stock-row"><div class="city">${esc(c)}</div><div class="qty">${Number(v)} шт.</div></div>`).join('');const specRows=s?Object.entries(s.specs||{}).map(([k,v])=>`<div class="spec-row"><div class="spec-key">${esc(k)}</div><div class="spec-value">${esc(v)}</div></div>`).join(''):'';$('#productView').innerHTML=`<div class="product-detail-head"><div><div class="eyebrow">${esc(s?.type||'Оборудование Netcraze')}</div><h1 class="detail-title">${esc(p.name)}</h1><div class="detail-sku">Артикул: ${esc(p.sku||'—')}</div></div><div class="detail-price">${money(p.price,p.currency)}</div></div><p class="detail-description">${esc(s?.description||'Характеристики модели будут добавлены позже.')}</p><div class="detail-grid"><section><h2>Характеристики</h2><div class="spec-table">${specRows||'<div class="spec-row"><div class="spec-key">Данные</div><div class="spec-value">Уточняются</div></div>'}</div></section><aside><div class="stock-card"><div class="stock-card-title">Наличие</div>${rows||'<div class="stock-row"><div class="city">Нет доступных остатков</div></div>'}<div class="stock-total"><div class="label">Всего доступно</div><div class="num">${total(p)} <small>шт.</small></div></div></div></aside></div>`}
init().catch(e=>{$('#productView').innerHTML='<div class="not-found">Не удалось загрузить карточку товара.</div>';console.error(e)});
